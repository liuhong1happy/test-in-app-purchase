# quickly start

```
# install flutter
git clone https://github.com/flutter/flutter.git
cd flutter
export PATH="$PATH:`pwd`/bin"
flutter doctor

# install dependencies
futter pub get
cd ios
pod install

# run app - open ios/Runner.xcworkspace
```


